plugins {
	id 'microservice'
}
apply plugin: 'info.solidsoft.pitest'

dependencies {
	implementation(project(":commons"))
	testImplementation(testFixtures(project(":commons")))
	implementation(project(":authentication"))
}

setupJacoco("nl.tudelft.sem.template.checkout.*")

jar {
	enabled = true
}